<h2>Pagamentos de <%= @student.name %> </h2>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Dia do Pagamento</th>
      <th>Valor</th>
    </tr>    
  </thead>
  <tbody>
    <% if !@payments.empty? %>
      <% @payments.each_with_index do |payment, i| %>
        <tr>
          <td style="text-align: center; width: 4%"><%= i+1 %></td>
          <td><%= l(payment.pay_day, :format => :date) %></td>
          <td><%= payment.value %></td>
        </tr>
      <% end %>
    <% else %>
    <tr>
        <td colspan="4" style="text-align: center">
          <font class="text-error" >Não há alunos cadastrados.</font>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div>
  <%= link_to "Voltar", student_home_path(@student), :class => 'btn btn-default'  %>
</div>